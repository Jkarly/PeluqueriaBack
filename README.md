# Proyecto Laravel

Este es un proyecto desarrollado con **Laravel**.

⚠️ **IMPORTANTE:** Si intentas abrir este proyecto directamente desde carpetas o haciendo doble click en los archivos, el `localhost` NO funcionará y te mostrará error.  
Esto es porque este proyecto **no funciona como páginas estáticas**, sino que Laravel necesita ejecutar su propio servidor para funcionar.  
Por lo tanto, este proyecto actúa solo como **cliente bajo el servidor de Laravel**, y es obligatorio levantarlo con `php artisan serve`.

---

## 🚀 Requisitos

- PHP 8 o superior
- Composer
- Postgres
- Node.js (opcional si usas Vite)

---

## 📦 Instalación

Clona el repositorio:

```bash
git clone <url-del-repo>
cd <nombre-del-proyecto>
